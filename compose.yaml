services:
  php:
    build:
      context: .
      dockerfile: Docker/Dockerfile
      args:
        LOCAL_UID: ${LOCAL_UID:-1000}
        LOCAL_GID: ${LOCAL_GID:-1000}
    working_dir: /app
    init: true
    command: ["sleep", "infinity"]
    volumes:
      - .:/app
